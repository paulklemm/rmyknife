% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment-verify.R
\name{project_verify}
\alias{project_verify}
\title{Check that everything needed to restore this project is in place}
\usage{
project_verify(path = ".", deep = FALSE, network = TRUE, strict = FALSE)
}
\arguments{
\item{path}{Project root.}

\item{deep}{Whether to verify image checksums. Several GB per image, so slow.}

\item{network}{Whether to probe the pinned repositories.}

\item{strict}{Whether to raise an error when any check fails. Used by the
makefile target.}
}
\value{
A tibble of \code{check}, \code{status} and \code{detail}, invisibly.
}
\description{
Reports two independent notions of restorability. \emph{From backup} is offline and
exact: the archived containers plus the binary library. \emph{From lockfile} is a
network rebuild from scratch and needs every package to resolve, which is
commonly partial for a project converted from a pre-renv state. A project can
be perfectly restorable from its backup while its lockfile is still messy.
}
\examples{
\dontrun{
  project_verify()
  project_verify(deep = TRUE, strict = TRUE)
}
}
