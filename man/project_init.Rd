% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment.R
\name{project_init}
\alias{project_init}
\title{Set up a reproducible environment for an analysis project}
\usage{
project_init(
  path = ".",
  image = Sys.getenv("APPTAINER_CONTAINER"),
  aux_images = character(),
  snapshot_date = NULL,
  bioc_version = NULL,
  bind = "/cephfs:/cephfs",
  dirs = c("analysis", "docs", "release"),
  checksum = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{path}{Project root. Defaults to the working directory.}

\item{image}{Path to the primary singularity image. Defaults to the image the
current session runs in.}

\item{aux_images}{Character vector of additional images used by the project.}

\item{snapshot_date}{CRAN snapshot date. Defaults to the date in an existing
\code{renv.lock}, else the image build date.}

\item{bioc_version}{Bioconductor release. Defaults to the version in an
existing \code{renv.lock}, else the running BiocManager version.}

\item{bind}{Bind mounts for the singularity invocation.}

\item{dirs}{Directories to create if missing.}

\item{checksum}{Whether to compute image checksums. Several GB per image over
a network filesystem, so roughly a minute each, once.}

\item{overwrite}{Whether to rewrite \code{environment.lock} if it already exists.}
}
\value{
The environment lock, invisibly.
}
\description{
Records the singularity image the project runs in, pins CRAN and Bioconductor
to dated snapshots, and initialises renv. Safe to run on an existing project:
nothing that is already present and correct is overwritten.
}
\details{
Must be run inside the image it records, because \code{\link[renv:init]{renv::init()}} builds the
package library with the running R. Recording one image from a session inside
a different one would produce a library that matches neither.

Auxiliary images (tool containers such as ggsashimi) are recorded and
checksummed for archival but never entered.
}
\examples{
\dontrun{
  # New project, inside the container:
  project_init()

  # Converting a project that uses a second tool container:
  project_init(aux_images = "/cephfs/.../ggsashimi_latest.sif")
}
}
